import React from "react";
import styled from "styled-components";

const CatalogItemRecommended = () => {
  return <Container></Container>;
};

const Container = styled.div`
  width: 20%;
  border: 1px solid black;
`;

export default CatalogItemRecommended;
